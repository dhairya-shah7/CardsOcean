export default function SellerPage() {
  return (
    <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8">
      <div className="glass-card rounded-[32px] p-6">
        <p className="text-sm uppercase tracking-[0.28em] text-royal-500">Seller dashboard</p>
        <h1 className="mt-2 text-4xl font-semibold text-slate-900">Listing controls and earnings overview</h1>
        <p className="mt-3 max-w-3xl text-slate-600">This shell is ready for upload, listing management, verification queue, sales analytics, and withdrawal requests once the seller endpoints are connected.</p>
      </div>
    </main>
  );
}

